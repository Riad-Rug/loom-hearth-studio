import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function AdminProductsPage() {
  return (
    <PlaceholderPanel
      title="Admin products placeholder"
      body="Product management workflows will be built against the shared product types and repository boundaries."
    />
  );
}
