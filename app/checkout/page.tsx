import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function CheckoutPage() {
  return (
    <PlaceholderPanel
      title="Checkout placeholder"
      body="Guest checkout is confirmed, but the exact Stripe integration pattern is intentionally unresolved."
    />
  );
}
