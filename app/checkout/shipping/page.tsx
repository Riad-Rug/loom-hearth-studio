import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function CheckoutShippingPage() {
  return (
    <PlaceholderPanel
      title="Checkout shipping placeholder"
      body="Launch shipping is fixed at $0.00. No shipping provider integration has been introduced at scaffold stage."
    />
  );
}
